(*This is a comment.*)

val x = 1;
(* dynamic environment. x --> 34 *)

val y = 17;

val z = x -  y;

val abs_of_z = if z < 0 then 0 - z else z; (* bool *)
(* dynamic environment : ...*)

val abs_z = abs z;
